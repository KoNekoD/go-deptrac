services:
  - class: Internal\Qossmic\Deptrac\IgnoreDependenciesOnContract
    tags:
     - { name: kernel.event_subscriber }

deptrac:
  paths:
    - ./pkg

  analyser:
    internal_tag: "@internal"
    types:
      - class
      - class_superglobal
      - file
      - function
      - function_superglobal
      - function_call

  formatters:
    graphviz:
      pointToGroups: true
      groups:
        Contract:
          - Contract
        Supportive:
          - Supportive
          - File
          - Time
          - DependencyInjection
          - OutputFormatter
          - Symfony
        Core:
          - Analyser
          - Ast
          - Dependency
          - InputCollector
          - Layer

  layers:
    # Domains
    - name: Analyser
      collectors:
        - type: directory
          value: pkg/core/analyser/.*
    - name: Ast
      collectors:
        - type: directory
          value: pkg/core/ast/.*
        - type: composer
          private: true
          composerPath: composer.json
          composerLockPath: composer.lock
          packages:
            - phpstan/phpdoc-parser
            - nikic/php-parser
            - phpdocumentor/type-resolver
    - name: Console
      collectors:
        - type: directory
          value: pkg/supportive/console/.*
    - name: Dependency
      collectors:
        - type: directory
          value: pkg/core/dependency/.*
    - name: DependencyInjection
      collectors:
        - type: directory
          value: pkg/supportive/dependency_injection/.*
    - name: Contract
      collectors:
        - type: directory
          value: pkg/contract/.*
    - name: InputCollector
      collectors:
        - type: directory
          value: pkg/core/input_collector/.*
    - name: Layer
      collectors:
        - type: directory
          value: pkg/core/layer/.*
    - name: OutputFormatter
      collectors:
        - type: directory
          value: pkg/supportive/output_formatter/.*
        - type: composer
          private: true
          composerPath: composer.json
          composerLockPath: composer.lock
          packages:
            - phpdocumentor/graphviz
    - name: File
      collectors:
        - type: directory
          value: pkg/supportive/file/.*
    - name: Time
      collectors:
        - type: directory
          value: pkg/supportive/time/.*
    - name: Supportive
      collectors:
        - type: bool
          must_not:
            - type: directory
              value: pkg/supportive/.*/.*
          must:
            - type: directory
              value: pkg/supportive/.*
    - name: Symfony
      collectors:
        - type: composer
          composerPath: composer.json
          composerLockPath: composer.lock
          packages:
            - symfony/config
            - symfony/console
            - symfony/dependency-injection
            - symfony/event-dispatcher
            - symfony/filesystem
            - symfony/finder
            - symfony/yaml

  ruleset:
    Layer:
      - Ast
      - Symfony
      - Contract
    Console:
      - Analyser
      - OutputFormatter
      - DependencyInjection
      - File
      - Time
      - Symfony
      - Contract
    Dependency:
      - Ast
      - Contract
      - DependencyInjection
    Analyser:
      - Layer
      - Dependency
      - Ast
      - Symfony
      - Contract
      - DependencyInjection
    OutputFormatter:
      - DependencyInjection
      - Symfony
      - Contract
    Ast:
      - File
      - InputCollector
      - Symfony
      - Contract
      - DependencyInjection
    InputCollector:
      - File
      - Symfony
    DependencyInjection:
      - Symfony
      - Console
      - Contract
    Contract:
      - Symfony
    File:
      - Symfony
